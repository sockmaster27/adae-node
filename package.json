{
    "name": "ardae-js",
    "version": "0.1.0",
    "description": "",
    "main": "index.node",
    "types": "index.d.ts",
    "scripts": {
        "build": "cargo-cp-artifact -nc index.node -- cargo build --message-format=json-render-diagnostics",
        "build-debug": "npm run build -- --features custom_debug_output",
        "build-record": "npm run build-debug record_output",
        "build-release": "npm run build -- --release",
        "install": "npm run build-release",
        "test": "concurrently \"cargo test\" \"npm:build-debug\" && jest"
    },
    "author": "",
    "license": "ISC",
    "devDependencies": {
        "@types/jest": "^28.1.6",
        "@types/node": "^18.0.5",
        "cargo-cp-artifact": "^0.1.6",
        "jest": "^28.1.3",
        "concurrently": "^6.5.1"
    }
}
